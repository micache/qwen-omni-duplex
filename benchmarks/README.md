# Benchmarks

Benchmark implementations are deferred beyond Session 01. `voicebench.py` and
`full_duplex_bench.py` reserve the command-line entry points without claiming
results.

Any Full-Duplex-Bench reporting in this repository is a **text-timeline
adaptation**, not an official speech-output score. Results must preserve that
label in tables, plots, and prose.
